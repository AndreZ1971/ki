version: '3.8'

services:
<<<<<<< HEAD
<<<<<<< HEAD
=======
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        VITE_API_URL: https://my-working-space.de/api
        VITE_VERSION: 1.0.0
    container_name: woo-frontend
    ports:
      - "80:80"
    restart: unless-stopped

  backend:
    build: ./backend
    container_name: woo-backend
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - DATABASE_URL=${DATABASE_URL}
      - JWT_SECRET=${JWT_SECRET}
    volumes:
      - ./backend/data:/app/data:rw

  watchtower:
    image: containrrr/watchtower
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    command: --interval 300 --cleanup
    restart: unless-stopped

>>>>>>> f6db666 (docker-compose.yml modifiziert. ESLint wird ignoriert.)
  nginx:
    version: '3.8'

    services:
      woo-nginx:
        image: nginx:latest
        container_name: woo-nginx
        ports:
          - "80:80"
          - "443:443"
        volumes:
          - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
          - /etc/letsencrypt:/etc/letsencrypt:ro
        depends_on:
          - woo-backend
        networks:
          - kaufe-es-network

      woo-backend:
        build:
          context: .
          dockerfile: Dockerfile
        container_name: woo-backend
        restart: unless-stopped
        expose:
          - "3000"
        environment:
          - NODE_ENV=production
        volumes:
          - ./backend/data:/app/data:rw
          - ./logs:/app/logs:rw
          - ./backend/.env.production:/app/.env:ro
        networks:
          - kaufe-es-network

      watchtower:
        image: containrrr/watchtower
        volumes:
          - /var/run/docker.sock:/var/run/docker.sock
        command: --interval 300 --cleanup
        restart: unless-stopped
        networks:
          - kaufe-es-network

    networks:
      kaufe-es-network:
        driver: bridge
    image: nginx:latest
    container_name: woo-nginx
=======
  nginx:
    image: nginx:alpine
    container_name: kaufe-es-nginx
    restart: unless-stopped
>>>>>>> 2d5cac2 (nginx.conf und gdocker-compose.yml modifiziert. ESLint wird ignoriert.)
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - /etc/letsencrypt:/etc/letsencrypt:ro
<<<<<<< HEAD
    depends_on:
<<<<<<< HEAD
<<<<<<< HEAD
      - backend
=======
      - frontend
      - backend
>>>>>>> b160c4f (docker-compose.yml modifiziert. ESLint wird ignoriert.)
=======
      - kaufe-es-agent
=======
      - ./frontend-dist:/usr/share/nginx/html:ro  # Falls Frontend vorhanden
>>>>>>> 4aeea4b (nginx.conf und docker-compose.yml modifiziert. ESLint wird ignoriert.)
    networks:
      - kaufe-es-network

  kaufe-es-agent:
    build: 
      context: .
      dockerfile: Dockerfile
    container_name: kaufe-es-agent
    restart: unless-stopped
    expose:
      - "3000"
    environment:
      - NODE_ENV=production
      - LOG_LEVEL=info
    env_file:
      - .env.production
    volumes:
      - ./data:/app/data:rw
      - ./logs:/app/logs:rw
    healthcheck:
      test: ["CMD", "node", "healthcheck.js"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - kaufe-es-network

networks:
  kaufe-es-network:
    driver: bridge
>>>>>>> 2d5cac2 (nginx.conf und gdocker-compose.yml modifiziert. ESLint wird ignoriert.)
