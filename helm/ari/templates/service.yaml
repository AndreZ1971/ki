apiVersion: v1
kind: Service
metadata:
  name: {{ include "ari.fullname" . }}
  labels:
    {{- include "ari.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "ari.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
